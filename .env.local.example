# Supabase Configuration (Required)
# Get these from your Supabase project settings: https://app.supabase.com
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# AI Gateway Configuration (Optional)
# If not configured, the app will attempt to use OpenAI API key directly
# AI_GATEWAY_URL is required if using AI Gateway
AI_GATEWAY_URL=https://your-ai-gateway-url.com
AI_GATEWAY_API_KEY=your-ai-gateway-api-key-here

# OpenAI Configuration (Optional fallback)
# Used if AI_GATEWAY_URL is not configured
OPENAI_API_KEY=sk-your-openai-api-key-here

# Azure OpenAI Configuration (Optional fallback)
# Used if both AI_GATEWAY_URL and OPENAI_API_KEY are not configured
AZURE_OPENAI_API_KEY=your-azure-openai-key-here

# AI Model Configuration (Optional)
# Defaults to "gpt-4o-mini" if not specified
# Can be overridden per request via API
AI_MODEL=gpt-4o-mini
AI_GATEWAY_MODEL=gpt-4o-mini

# Node Environment (Default: development)
# Set to "production" for production deployments
NODE_ENV=development
